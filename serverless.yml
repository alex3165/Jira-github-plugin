service: api

provider:
  name: aws
  runtime: nodejs8.10

# you can add packaging information here
package:
 include:
   - lambda.js
   - handlers.js
   - requests.js
   - node_modules/**

functions:
  get-monuments:
    handler: lambda.pullRequest
    environment:
